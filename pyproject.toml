[project]
name = "erpnext-core"
# dynamic version will be set by flit
dynamic = ["version"]
description = "Minimal ERPNext Core"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["frappe>=16,<17"]

[tool.flit.module]
name = "erpnext"
