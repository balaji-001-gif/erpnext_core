[project]
name = "erpnext"
# dynamic version will be set by flit
dynamic = ["version"]
description = "Minimal ERPNext Core"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.flit.module]
name = "erpnext"
